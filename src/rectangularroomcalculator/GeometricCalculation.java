package rectangularroomcalculator;

public interface GeometricCalculation {
    void calculateArea(double roomHeight, double roomWidth);
    void calculatePerimeter(double roomHeight, double roomWidth);
}
